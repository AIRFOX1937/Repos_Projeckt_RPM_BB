@{
    ViewData["Title"] = "Преподаватели и Дисциплины";
    Layout = "~/Views/Shared/_LayoutA.cshtml";
}
@model List<BBLib.Models.UsDi>
<div class="container" style="margin-top: 40px">
    <h1 style="margin-left: auto; margin-right: auto; margin-bottom: 30px;">
        Преподаватели и Дисциплины
    </h1>
    <a asp-action="AddUD" class="btn btn-primary" style="margin-bottom: 20px; width: 200px; font-size: 18px; color: white">Добавить</a>
    <table class="table table-hover" style="font-size:18px">
        <thead>
            <tr>
                <th scope="col">№</th>
                <th scope="col">ФИО преподавателя</th>
                <th scope="col">Название дисциплины</th>
                <th scope="col"></th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>
                        @user.IdUD
                    </td>
                    <td>
                        @user.FioU
                    </td>
                    <td>
                        @user.NameD
                    </td>
                    <td>
                        <a href="@Url.Action("EditUD", new { id = user.IdUD })" style="margin-top: 20px">Изменить</a>
                    </td>
                    <td>
                        <form asp-action="DeleteUD" asp-route-id="@user.IdUD" method="post">
                            <input type="submit" value="Удалить" style="border-color:transparent; background-color:transparent" />
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>